# Port to run the API on
PORT=3001

# API key required via the `x-api-key` header
API_KEY=dev-api-key

# Optional comma-separated origin allow list for CORS
# Example: https://your-account.github.io,https://example.com
ALLOWED_ORIGINS=

# Optional absolute path to the SQLite file. Useful on Render where
# `/var/data` is backed by a persistent disk.
# DATABASE_FILE=/var/data/pa-shift-data.sqlite
